# 咱家菜谱：长期开发规范

本文件是本项目的永久开发规范。除非用户明确要求，否则不得修改本文件。开始任何开发前，必须先阅读本文件、`BUSINESS_RULES.md` 和 `ARCHITECTURE.md`。

## 项目定位

- 咱家菜谱是家庭私房菜谱 PWA，不是公开内容平台、社交产品或商业后台。
- 手机竖屏优先，强调大字体、少操作、清晰阅读和长期积累。
- 线上正式地址保持为 `https://zanjia-caipu.vercel.app`，不得把本项目部署到原分租项目或改变正式域名，除非用户明确授权。

## 技术边界

- 当前前端是原生 ES Module JavaScript，不引入 React、Flutter 或其他框架，除非用户明确批准迁移。
- 静态资源位于根目录、`src/`、`icons/`、`splash/`；构建脚本复制到 `dist/`。
- 服务端 API 位于 `api/`，公共服务端工具位于 `lib/`，本地开发服务器为 `server.cjs`。
- Supabase 是生产数据与图片服务；服务端使用 Service Role Key，密钥只能通过环境变量提供，绝不得提交到 Git。
- `localStorage`、IndexedDB、Cache Storage 和 Service Worker 只用于本地缓存/离线体验，不能替代服务器事实数据。

## UI 与交互原则

- 采用《咱家》系列统一交互：右上角固定主题、分享、菜单三个入口；一个功能只保留一个入口。
- 首页默认显示当前账号的“我的菜谱”；共享菜谱通过“家庭共享”进入；管理员查看成员菜谱必须主动进入成员管理。
- 详情页默认完整展开材料、调料、步骤、注意事项、备注和做菜记录；页面只允许正常纵向滚动，不新增自定义横向页面切换。
- 表单与详情页必须兼容浅色/深色主题变量，保证标题、正文、辅助文字、输入框和按钮具有足够对比度。
- 保持图片 4:3 展示、`object-fit: cover`，不使用会改变图片亮度的滤镜或过渡。

## 编码、数据和安全

- 所有源文件必须保存为 UTF-8（无 BOM）；禁止用 ANSI、GBK 或 Latin-1 重写中文。
- HTML 必须声明 UTF-8；API JSON 必须发送 `application/json; charset=utf-8`。
- 不在源码、日志、提交信息或文档中写入管理员密码、PIN、Service Role Key 或其他密钥。
- 任何新增读写 API 都必须同时实现会话校验、家庭隔离、角色权限和错误响应。
- 游客只能读取 `is_family_shared = true` 的菜谱及其允许公开的详情/留言接口，不能调用任何写入或管理接口。

## 固定开发流程

1. 先阅读三份规范文件并检查现有实现，不凭记忆重写功能。
2. 明确本次变更涉及的业务规则、架构文件和回归范围。
3. 只做用户要求范围内的修改；不得顺手改变数据模型、权限或部署平台。
4. 修改后运行至少 `node --check src/main.js` 和 `npm.cmd run build`；有 API 变化时补充接口检查。
5. 提交前执行完整回归：启动、登录（管理员/成员/游客）、退出、新增、编辑、删除、详情、图片、搜索、分类、我的菜谱、家庭共享、收藏、备注、做菜记录、成员管理、管理员权限、普通成员权限、PWA 更新和深色模式。
6. 每次完成后追加 `CHANGELOG.md`；新增业务规则同步 `BUSINESS_RULES.md`；结构变化同步 `ARCHITECTURE.md`。
7. 发布前确认正式 Vercel 网址真实打开并可操作，不以 HTTP 200 或构建成功代替页面验收。

## 变更报告

每次交付必须说明：修改文件、新增功能、修复问题、已通过测试、未完成或未能真实验证的项目。任何回归失败都必须先修复，不继续扩展功能。

