# 咱家菜谱业务规则

## 产品范围

这是家庭内部长期使用的私房菜谱库。核心对象是菜谱、图片、历史备注、做菜记录和家庭共享；不开放公开注册，不提供商城、会员、社交关注等平台化功能。

## 账号与角色

系统当前支持三种会话角色：

- `admin`：家长/管理员。使用管理员邮箱与环境变量密码登录；可查看和管理家庭成员、查看全家菜谱、管理共享状态，并可编辑或删除成员菜谱。
- `member`：家庭成员。由管理员创建，使用账号编号和 PIN 登录；只能管理自己创建的菜谱，且可查看家庭共享菜谱。
- `guest`：游客。无需注册即可进入；只能查看家庭共享菜谱、共享详情和留言，不能新增、编辑、删除、收藏、复制、记录做菜或进入成员管理。

账号不支持公开注册。成员 PIN 只保存为哈希值，不保存明文。会话使用 HttpOnly Cookie，默认有效期 30 天。

## 菜谱可见性

- 新菜谱默认属于创建者的私人菜谱，`is_family_shared` 默认关闭。
- 普通成员首页默认只显示自己创建的菜谱；家庭共享入口显示所有已开启共享的菜谱。
- 管理员首页同样默认只显示自己的菜谱；查看其他成员菜谱必须从成员管理主动进入。
- 游客和成员都可以查看共享菜谱详情；私人菜谱只能由创建者和管理员查看。
- 共享菜谱同时保留在创建者的“我的菜谱”和“家庭共享”列表中。

## 编辑与删除权限

- 菜谱创建者可以编辑、删除自己的菜谱、修改共享状态、维护备注和做菜记录。
- 管理员可以编辑、删除任意家庭菜谱并管理共享状态。
- 普通成员不能编辑或删除别人的共享菜谱；可以查看、收藏，且可复制为自己的菜谱。
- 游客没有任何菜谱写入权限。
- 删除菜谱必须先完成数据库变更；图片清理必须遵循图片安全规则，不得误删仍被引用的文件。

## 菜谱内容

菜谱至少包含：菜名、分类、统一材料清单（食材和调味品不再分栏）、制作步骤、注意事项、历史备注、图片、作者、家庭、创建/修改时间、共享状态、收藏信息、做菜次数与最近做菜时间。数据库暂时保留 `seasonings` 兼容字段，但新界面和新写入统一使用 `ingredients`。

固定分类为：热菜、凉菜、汤类、主食、粥类、甜品、肉菜、素菜；一个菜可以多选分类。

数据库目前仍保留 `tags` 字段以兼容历史数据，但当前表单和筛选不应重新暴露标签模块，除非用户明确恢复。

## 备注、做菜记录与留言

- 历史备注按日期倒序显示，每条支持创建者/管理员编辑和删除。
- 做菜记录保存日期、备注、图片和评分；按创建者/管理员权限支持编辑和删除；做菜次数由记录数量维护。
- 每次做菜可以保存图片，用于成长记录；不能因此修改原菜谱内容。
- 游客只能对共享菜谱留言，必须提供昵称和非空内容，单条最多 300 字；游客不能编辑/删除留言。
- 菜谱创建者和管理员可以删除对应共享菜谱的游客留言。

## 图片规则

- 上传前在浏览器统一转为普通 JPEG，最大边约 1600px，质量约 0.82，去除原始 EXIF/HDR/Display P3 信息，降低 iPhone HDR 闪烁风险。
- 图片在 Supabase Storage 中按 `imageId` 保存，前端通过 `/api/images` 受会话权限保护地读取。
- 图片替换必须先上传新图并成功保存数据库引用，再处理旧图；任一步失败都保留旧图。
- 删除菜谱或图片不得留下误删风险。当前自动清理接口是 dry-run，线上 Storage 删除保持暂停，必须先确认引用关系。
- 清理候选必须排除所有菜谱主图、做菜记录图和历史图片引用；新上传文件至少保留 24 小时。

## 缓存与同步

## 长期登录规则（2026-07-18）

- 保留既有 localStorage 用户缓存键 `family-recipes-last-user`，不得随意改名或清除。
- 服务端签名 HttpOnly 会话默认有效期为一年，并在成功验证时滚动续期；Access Token 不被改成长效令牌。
- 初始化、断网、网络抖动、5xx 或暂时无法验证时，不得清除现有用户状态。
- 只有 `/api/auth` 明确返回 `401` 且 `reason=session_expired`，或用户主动执行退出，才进入登录页。
- 浏览器 Supabase Auth 客户端启用 `persistSession`、`autoRefreshToken`、`detectSessionInUrl`，不设置 `storageKey` 以保留默认 key；初始化时的 `session=null` 不是退出事件。

- 用户菜谱元数据按用户缓存到 `localStorage`/IndexedDB；图片响应使用 Cache Storage/IndexedDB。
- 菜谱显示排序使用当前用户/当前设备保存的本地打开序号：最近真正打开的菜谱优先，其余按原有创建时间顺序稳定排列。共享菜谱不会因为其他成员打开而改变本设备顺序；图片加载、收藏、共享、备注、做菜记录和后台同步不得改变打开顺序。
- 新增菜谱保存成功后写入最新本地打开序号并排在第一位；打开详情页只更新当前设备的本地序号，不再更新全局排序字段。
- 启动先显示本地缓存，再后台同步 Supabase；下拉刷新才执行主动完整同步。
- Service Worker 可以缓存静态壳和图片，但不能让旧 HTML/JS 无限覆盖新版本；发布时必须更新缓存版本并保留图片缓存。
- 图片缓存键使用稳定的 `imageId + imageVersion`，有效图片不自动过期或自动清理；只有图片更换、路径/版本变化、本地缓存不存在或用户主动清除缓存时才重新下载。
- 设置菜单中的“清除本地缓存”只清除当前设备菜谱快照、打开顺序、IndexedDB 图片、图片响应缓存和旧静态资源缓存，保留登录状态，不删除服务器菜谱或 Supabase Storage 图片，完成后重新同步服务器数据。
- 管理员设置菜单可查看当前 `recipe-images` bucket 的图片数量和扫描到的总容量；若配置 `SUPABASE_STORAGE_CAPACITY_BYTES`，达到 70% 会提示管理员；当前不执行自动迁移或自动删除。
